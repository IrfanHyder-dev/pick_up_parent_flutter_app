enum SnackBarType {
  error,
  success,
  universal,
  warning,
}

enum RideShiftType {
  morning,
  evening,
  not_available,
}

enum ArrivalStatus {
  not_arrived,
  arrival_soon,
  arrived,
  picked,
  dropped,
  not_going,
}

enum RideStatus {
  started,
  not_started,
  finished,
}

enum QuotationStatus {
  paid,
  accept,
  reject,
  pending,
  accept_and_paid,
  cancel,
}

enum PaymentStatus{
  paid,
  un_paid,
  pending,
}
